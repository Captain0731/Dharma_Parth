{
  "version": 2,
  "buildCommand": "cd client && npm install && CI=false GENERATE_SOURCEMAP=false DISABLE_ESLINT_PLUGIN=true npm run build",
  "outputDirectory": "client/build",
  "installCommand": "cd client && npm install",
  "framework": null
}
