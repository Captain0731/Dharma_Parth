{
  "version": 2,
  "buildCommand": "cd client && npm install && DISABLE_ESLINT_PLUGIN=true CI=false npm run build",
  "outputDirectory": "client/build",
  "installCommand": "cd client && npm install",
  "framework": null
}
